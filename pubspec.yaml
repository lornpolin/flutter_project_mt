name: bank_simulation
description: A simple bank simulation console application in Dart.
version: 1.0.0
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  # No external runtime dependencies required.

dev_dependencies:
  test: ^1.24.0
